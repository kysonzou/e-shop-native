# .envrc

# Load local, git-ignored environment variables if the file exists.
# This should be at the top.
source_env_if_exists .envrc.local

# Set up Go environment using direnv's stdlib
layout go

# Add project's local bin directory to the PATH.
# This allows using tools installed via 'make tools'.
export PATH=$(pwd)/bin:$PATH

echo "e-shop-native environment activated."
echo "Tools from ./bin are now available in your PATH."